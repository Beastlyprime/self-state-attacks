# Available Tools

## Session Management
- `session_list`: List sessions under the configured session store
- `session_archive`: Archive a session to cold storage

## Shell Access
- `shell_exec`: Execute shell commands with timeout and safety validation
